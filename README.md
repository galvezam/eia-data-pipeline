### EIA Data Pipeline
Register for an API here: ...

### Ingestion
- Note that the jupyter notebook has ingestion for the year of 2025. We will add more data for previous years to get that amount of data similar to that in the ```target_data/``` directory. We will run a batch job to get data from the previous months for the data that allows it (some data only comes annually).
- We will add processing layers in the weeks to come and a dashboard to visualize the data, either in time series graphs or on a map of the US.
